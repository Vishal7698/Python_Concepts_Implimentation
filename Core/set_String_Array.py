#set are unmutable and dont allow dublicates

lang_set={"Java","Python","go"}
print(lang_set)
print(len(lang_set))
#methods are add , remove, discard

#for string
str="hello"
str1='world'
str=str+str1
print(str)

#python does not support array as default we need import array module to create array
import array as arr

arrayOfInteger=arr.array('i',[1,2,5,6])
print(arrayOfInteger)
# i is typecode for integer. we can use as per our need